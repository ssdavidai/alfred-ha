{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Alfred Black",
        "description": "Point this Home Assistant at an Alfred tenant. Mint a channel token from `/study` (or `POST /api/v1/channel-tokens/mint` with channel `ha-conversation`) and paste it below.",
        "data": {
          "base_url": "Alfred tenant URL",
          "channel_token": "Channel token"
        },
        "data_description": {
          "base_url": "Base URL of your Alfred deployment (https scheme expected)",
          "channel_token": "Starts with `ha_` followed by 48 hex characters."
        }
      }
    },
    "error": {
      "invalid_url": "Base URL must be a full https://… or http://… address.",
      "invalid_token_shape": "Channel token must start with `ha_` followed by 48 hex characters.",
      "invalid_auth": "Alfred rejected the token. Mint a fresh one from /study and try again.",
      "cannot_connect": "Could not reach Alfred at that URL. Check the host and your network (Tailscale, etc.)."
    },
    "abort": {
      "already_configured": "This Alfred tenant is already connected to Home Assistant."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Alfred Black options",
        "description": "Tune per-turn behaviour for this Alfred tenant.",
        "data": {
          "timeout": "Per-turn timeout (seconds)"
        },
        "data_description": {
          "timeout": "How long to wait for a single conversation turn before giving up. Tool-using turns (calendar, vault search, integrations chained through Composio) routinely take 25-45s on a cold path; the 90s default leaves slack. Lower for fast networks; raise for slow ones."
        }
      }
    }
  }
}
